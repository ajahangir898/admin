# Frontend/Admin env example (DO NOT put secrets here)
# Copy to .env (and optionally .env.production) and fill values.

# API base for client requests
VITE_API_BASE_URL=https://your-domain.com

# Optional: CDN settings
VITE_CDN_ENABLED=false
VITE_CDN_PROVIDER=custom
VITE_CDN_BASE_URL=https://cdn.your-domain.com
VITE_CDN_IMAGE_URL=https://images.your-domain.com
VITE_CDN_STATIC_URL=https://static.your-domain.com
VITE_CDN_IMAGE_TRANSFORM=true
VITE_CDN_IMAGE_QUALITY=80
VITE_CDN_IMAGE_FORMAT=auto

# Optional: Feature flags / tuning
VITE_REMOTE_SAVE_DEBOUNCE_MS=1200
VITE_DISABLE_REMOTE_SAVE=false

# IMPORTANT
# Any variable prefixed with VITE_ is bundled into the browser and is public.
# Do not store DB passwords / JWT secrets in frontend env files.
